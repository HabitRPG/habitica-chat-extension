'use strict';

const loadChat = require('../../../src/js/pages/chat/load-chat');

describe('loadChat', function () {
  it('returns early if there are no settings', function () {
    // TODO, fill in
    loadChat();
  });
});

